;;; config-prog-mode.el --- summary -*- lexical-binding: t -*-

(add-hook 'prog-mode-hook
          (lambda ()
            (setq show-trailing-whitespace t)))



(provide 'config-prog-mode)

;;; config-prog-mode.el ends here
