;;; config-keymaps.el --- Keymaps -*- lexical-binding: t; -*-

(define-prefix-command 'svjson/m-leader-map)
(global-set-key (kbd "M-m") 'svjson/m-leader-map)

(provide 'config-keymaps)

;;; config-keymaps.el ends here
