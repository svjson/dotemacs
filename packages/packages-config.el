;;; packages-config.el --- Package manager config -*- lexical-binding: t; -*-

(require 'config-checkdoc)
(require 'config-package)
(require 'config-package-lint)
(require 'config-straight)

(provide 'packages-config)

;;; packages-config.el ends here
