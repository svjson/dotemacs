;;; config-dockerfile.el --- User interface customizations -*- lexical-binding: t; -*-

(add-to-list 'auto-mode-alist '("\\Dockerfile\\.*\\'" . dockerfile-ts-mode))

(provide 'config-dockerfile)

;;; config-dockerfile ends here
